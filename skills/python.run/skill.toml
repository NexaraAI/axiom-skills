id = "python.run"
name = "Run Python Safely"
version = "0.1.0"
description = "Explains how to run Python safely via the terminal allowlist. Guidance only; execution asks for approval."
category = "coding"
skill_type = "prompt"
risk_level = "low"
permissions = []
platforms = ["windows", "linux", "macos"]
entrypoint = "prompt-only"
author = "Axiom Agent"
license = "MIT"
min_axiom_version = "0.1.0"
keywords = ["python", "run", "execute", "test", "pytest", "script"]

[llm_card]
summary = "Use this when the user asks to run or test Python code."
when_to_use = ["User asks to run Python", "User asks to execute tests for a Python script", "User asks to debug a Python run or traceback"]
input_contract = "command, working_directory, safety_level"
output_contract = "exit_code, stdout, stderr"
token_budget = 260
