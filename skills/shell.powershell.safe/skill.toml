id = "shell.powershell.safe"
name = "Run Safe PowerShell"
version = "0.1.0"
description = "Guidance for safe PowerShell terminal tasks on Windows. High risk: commands ask for approval and stay inside the workspace."
category = "shell"
skill_type = "prompt"
risk_level = "high"
permissions = []
platforms = ["windows"]
entrypoint = "prompt-only"
author = "Axiom Agent"
license = "MIT"
min_axiom_version = "0.1.0"
keywords = ["powershell", "shell", "terminal", "command", "windows", "run"]

[llm_card]
summary = "Use safe PowerShell command execution for terminal tasks on Windows."
when_to_use = ["User asks to run a Windows terminal command", "User asks to test or inspect via PowerShell"]
input_contract = "command, working_directory, safety_level"
output_contract = "exit_code, stdout, stderr"
token_budget = 260
