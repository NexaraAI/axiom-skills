id = "shell.zsh.safe"
name = "Run Safe Zsh"
version = "0.1.0"
description = "Guidance for safe zsh terminal tasks on macOS. High risk: commands ask for approval and stay inside the workspace."
category = "shell"
skill_type = "prompt"
risk_level = "high"
permissions = []
platforms = ["macos"]
entrypoint = "prompt-only"
author = "Axiom Agent"
license = "MIT"
min_axiom_version = "0.1.0"
keywords = ["zsh", "shell", "terminal", "command", "macos", "run"]

[llm_card]
summary = "Use safe zsh command execution for terminal tasks on macOS."
when_to_use = ["User asks to run a macOS terminal command", "User asks to test or inspect via zsh"]
input_contract = "command, working_directory, safety_level"
output_contract = "exit_code, stdout, stderr"
token_budget = 260
